using System;

class Program
{
    public static void Main()
    {
        // Save all of the command line arguments in an `arguments` variable
        string[] arguments = Environment.GetCommandLineArgs();

        Console.WriteLine("Console");
    }
}